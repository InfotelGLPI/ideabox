{#
# -------------------------------------------------------------------------
# Ideabox plugin for GLPI
# -------------------------------------------------------------------------
#
# LICENSE
#
# This file is part of Ideabox.
#
# Ideabox is free software; you can redistribute it and/or modify
# it under the terms of the GNU General Public License as published by
# the Free Software Foundation; either version 2 of the License, or
# (at your option) any later version.
#
# Ideabox is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY; without even the implied warranty of
# MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
# GNU General Public License for more details.
#
# You should have received a copy of the GNU General Public License
# along with Ideabox. If not, see <http://www.gnu.org/licenses/>.
# -------------------------------------------------------------------------
# @copyright Copyright (C) 2026-2026 by Ideabox plugin team.
# @license   GPLv2 https://www.gnu.org/licenses/gpl-2.0.html
# @link      https://github.com/InfotelGLPI/Ideabox
# -------------------------------------------------------------------------
#}

{% import "components/form/fields_macros.html.twig" as fields %}
{% set field_options = {
    'disabled': true
} %}
{% set bg = "" %}
{% if item.isDeleted() %}
    {% set bg = "asset-deleted" %}
{% endif %}

<div class="asset {{ bg }}">
    {% include("components/form/header.html.twig") %}

    {% set rand = random() %}
    {% set params  = params ?? [] %}
    {% set target       = params['target'] ?? item.getFormURL() %}
    {% set withtemplate = params['withtemplate'] ?? "" %}
    {% set item_type = item.getType() %}
    <input type="hidden" name="users_id" value="{{ params['users_id'] }}" />
    <input type="hidden" name="date_comment" value="{{ params['date_comment'] }}" />
    <input type="hidden" name="plugin_ideabox_ideaboxes_id" value="{{ params['plugin_ideabox_ideaboxes_id'] }}" />
    <div class="card-body d-flex">
        <div class="col-9 flex-column">
            <div class="d-flex flex-row flex-wrap flex-xl-nowrap">
                <div class="row flex-row align-items-start flex-grow-1">
                    <div class="row flex-row">

                        {% if item.isField('name') %}
                            {{ fields.autoNameField(
                                "name",
                                item,
                                (item_type == 'Contact' ? __('Surname') : __('Name')),
                            withtemplate,
                            {
                                'required': true

                            }
                            ) }}
                        {% endif %}

                        {% if item.isField('users_id') %}
                            {{ fields.dropdownField(
                                "User",
                                "users_id",
                                params['users_id'],
                                __('Author'),
                                {
                                    'entity': item.fields['entities_id'],
                                    'right': 'all',
                                    'disabled': true
                                }
                            ) }}
                        {% endif %}

                        {{ fields.datetimeField(
                            'date_comment',
                            params['date_comment'],
                            __('Date of comment', 'ideabox'),
                            field_options
                        ) }}


                        {% if item.isField('comment') %}

                            {{ fields.textareaField(
                                'comment',
                                item.fields['comment'],
                                __('Description', 'ideabox'),
                                {
                                    'full_width': true,
                                    'enable_richtext': true,
                                    'is_horizontal': false,
                                    'enable_fileupload': false,
                                    'enable_images': false,
                                    'enable_mentions': false,
                                    'rand': rand,
                                    'required': true

                                }
                            ) }}
                        {% endif %}

                    </div> {# .row #}
                </div> {# .row #}
            </div> {# .flex-row #}
        </div>
        <div class="col-3 flex-column">
            <div class="flex-row asset-pictures">
                {% include("components/form/pictures.html.twig") with {'gallery_type': ''} %}
            </div>
        </div>
    </div> {# .card-body #}

    {% include("components/form/buttons.html.twig") %}
    {#    {% include("components/form/inventory_info.html.twig") %} #}


    {% if params['formfooter'] == null %}
        <div class="card-footer mx-n2 mb-n2 mt-4">
            {% include("components/form/dates.html.twig") %}
        </div>
    {% endif %}
</div>
